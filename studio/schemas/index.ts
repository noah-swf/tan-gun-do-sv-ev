import blockContent from './blockContent'
import post from './post'
import heroSection from './heroSection'
import trainingszeiten from './trainingszeiten'

export const schemaTypes = [post, blockContent, heroSection, trainingszeiten]
